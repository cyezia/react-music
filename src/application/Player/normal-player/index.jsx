import React from 'react';
import { getName } from '../../../api/utils';
import { NormalPlayerContainer, Top, Middle, Bottom, CDWrapper, Operators } from './style';

function NormalPlayer(props) {
  const { song } = props;
  return (
    <NormalPlayerContainer>
      
    </NormalPlayerContainer>
  )
}

export default React.momo(NormalPlayer);